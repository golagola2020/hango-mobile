<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InfoUpdateActivity"></activity>
        <activity android:name=".InfoActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UpdateVendingActivity" />
        <activity android:name=".DrinkMainActivity" />
        <activity android:name=".GuideDrinkMainActivity"
            android:theme="@style/Transparent">

        </activity>
        <activity android:name=".DrinkRefreshAcceptActivity"
            android:theme="@style/Transparent">

        </activity>
        <activity android:name=".UpdateDrinkActivity"
            android:theme="@style/Transparent">

        </activity>

        <activity android:name=".AddDrinkActivity"/>
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>